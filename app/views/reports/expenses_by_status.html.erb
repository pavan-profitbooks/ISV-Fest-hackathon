<%= render 'layouts/report_filters', path: expenses_by_status_reports_path, start_date: @start_date, end_date: @end_date %>
<%= render 'layouts/status_breakdown', status_breakdown: @status_breakdown, status_counts: @status_counts, total: @total %>

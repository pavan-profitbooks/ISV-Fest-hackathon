<%= render 'layouts/report_filters', path: expenses_by_vendor_reports_path, start_date: @start_date, end_date: @end_date %>
<%= render 'layouts/vendor_breakdown_table', data: @vendor_breakdown, total: @total %>
